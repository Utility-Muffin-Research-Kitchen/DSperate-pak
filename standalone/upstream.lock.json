{
  "schema": 1,
  "note": "Everything needed to reproduce the shipped DSperate binary. A fresh clone of this repository plus Docker is the whole toolchain: no sibling checkout, no locally built image, no opaque downloaded binary. DSperate is built from the pinned commit with the pak's patches in `patches/`, applied in order; each is locked by sha256 and the build refuses a mismatch. `make standalone` refuses a source archive or artifact whose sha256 does not match this file.",
  "core": {
    "id": "dsperate",
    "source_url": "https://github.com/beebono/DSperate.git",
    "source_tag": "v2.1.1",
    "source_commit": "baec96501802bb04203cac07b420c67eff8054b8",
    "license": "GPL-3.0-or-later",
    "license_path_in_source": "LICENSE"
  },
  "build": {
    "backend": "sdl",
    "sdl2": "device-provided libSDL2-2.0.so.0, linked dynamically; the MLP1 ships 2.28.5 built with its Wayland video driver",
    "wayland_define": "-DSDL_VIDEO_DRIVER_WAYLAND=1",
    "wayland_note": "The toolchain sysroot's SDL2 2.28.5 is built without its Wayland driver, so its SDL_config.h leaves SDL_VIDEO_DRIVER_WAYLAND undefined and SDL_syswm.h hides SDL_SysWMinfo's Wayland fields. Defining the macro for this build exposes those fields without changing the toolchain, and is what lets the intended dmabuf tier compile. The public union reserves the fields' space regardless, and the binary links the device's Wayland-capable SDL at runtime. The build fails if the probe or display_wl.cpp does not confirm the real tier.",
    "language": "C++17, upstream CMake/Ninja build",
    "source_date_epoch": 1789871851,
    "source_date_note": "The pinned v2.1.1 commit's committer timestamp.",
    "cheevos_version": "2.1.1",
    "cheevos_version_note": "Passed explicitly because a shallow checkout has no tags for upstream's `git describe` fallback.",
    "pgo": "use",
    "pgo_note": "A fresh aarch64 profile was trained with this pak's own toolchain (Buildroot GCC 12.3.0) and these flags against the patched v2.1.1 source, so its fingerprint matches the release build. Upstream v2.1.1 ships profiles for GCC 10.5 and 12.4.0 only, which this toolchain refuses.",
    "cmake": [
      "-DCMAKE_BUILD_TYPE=RelWithDebInfo",
      "-DCMAKE_TOOLCHAIN_FILE=<standalone>/mlp1-toolchain.cmake",
      "-DCMAKE_CXX_FLAGS=-DSDL_VIDEO_DRIVER_WAYLAND=1",
      "-DDSPERATE_TESTS=OFF",
      "-DDSPERATE_HEADLESS=OFF",
      "-DDSPERATE_CHEEVOS=ON",
      "-DDSPERATE_WAYLAND=ON",
      "-DDSPERATE_CHEEVOS_VERSION=2.1.1",
      "-DDSPERATE_PGO=use",
      "-DDSPERATE_PGO_STRICT=ON",
      "-DDSPERATE_PGO_DIR=/standalone/pgo/aarch64"
    ]
  },
  "pgo": {
    "dir": "standalone/pgo/aarch64",
    "sha256": "cd37cc840f216c5cc1cf79fbf3ce901796fe94ab94ca0381657ef9637b425d63",
    "files": 98,
    "fingerprint": "e5053f2d1f5b27e9ed70bda94b614845979aa599",
    "compiler": "12.3.0 (aarch64-buildroot-linux-gnu)",
    "source_commit": "baec96501802bb04203cac07b420c67eff8054b8",
    "date": "2026-09-21T20:20Z",
    "scenes": "mlbis sm64 etody dbori meteos gsdd nsmb",
    "note": "Trained with this pak's toolchain and flags against the patched v2.1.1 tree, so the fingerprint matches the release build. The scenes are upstream's recorded replays; `st` was skipped because its save state is for another ROM revision, which the loader refuses. The headless frontend is the training build; the SDL frontend, rcheevos and the reference kernels are never trained. Verify with -DDSPERATE_PGO_STRICT=ON: 0 unexpected objects without a profile and 0 coverage mismatches."
  },
  "version": {
    "tag": "v2.1.1",
    "commit": "baec965",
    "note": "Locked --version identity. The build exports DSPERATE_LOCK_VERSION and DSPERATE_LOCK_COMMIT, and cmake/version.cmake prefers them over git, so a source archive without git and a patched checkout git would call dirty both report v2.1.1 (baec965)."
  },
  "patches": [
    {
      "file": "0001-pak-cache-and-archive-policy.patch",
      "sha256": "f2f130ffe23c804dc39d222fac008d0931fe3810452c109c30953625457221de",
      "note": "Adds --cache-root-only, --single-rom and --inspect-cart, backed by cart.cache_root_only and cart.single_nds, plus unsafe entry-path rejection. Rebased onto v2.1.1, whose find_nds was renamed to find_rom and whose multi-entry pick now follows the game database; the policy features are still absent upstream, so they are carried. The pak's wrapper pins the extraction cache to its own root, requires exactly one image entry, and sizes the cache before launch. See PROVENANCE.md."
    },
    {
      "file": "0002-save-durability.patch",
      "sha256": "714c6d0494dee3e5ffa3a769acf26da628fbe3b19f1364b04d11ce4d35bc929a",
      "note": "Keeps checked flush, fsync, close and rename for battery-save and save-state writes, plus checked firmware-override flush and close. Retains v2.0.0 upstream save loading, chip detection and mismatch backups instead of the old fixed-size load guard. See PROVENANCE.md."
    },
    {
      "file": "0003-lid-resume-no-fabricated-close.patch",
      "sha256": "d25a4566fb4dfa1bb53a83fb89bf91c5e66eef2bc618f4bbfd45b470f8b738ef",
      "note": "Stops the host-resume lid pulse on a device with no lid switch. The old code fabricated a lid close/open cycle on every resume; games that blank their screens on lid-close and restore them only on an open they saw coming (Contra 4) stayed black. The pulse now only fires for a lid already believed closed. See PROVENANCE.md."
    },
    {
      "file": "0004-deterministic-version.patch",
      "sha256": "fc2cdbf8b04abc2af4423f2bca6b9dfc31c83db3def752b3fa21317c36da9bec",
      "note": "Makes --version report the lock's tag and commit deterministically. Upstream's generator derives the tag from git describe and the commit from git rev-parse, so a source archive without git says unknown and a patched checkout says <hash>-dirty. This patch prefers DSPERATE_LOCK_VERSION and DSPERATE_LOCK_COMMIT when the packager sets them, and leaves ordinary git development unchanged."
    },
    {
      "file": "0005-dsperate-ra-account-adapter.patch",
      "sha256": "efe21d16440933ce1f337a9fd91bbaf5226b22abb39f2297c8b4ccce013ab4fc",
      "note": "The standalone-ra-account-v1 consumer. Adds the shared contract classifier and marker (verbatim with the Flycast consumer and the leaf-contracts reference), the managed-account bridge, the native login wiring (import, one password retry, sign-out), and native credential persistence. Consumes UMRK_RA_ACCOUNT_* after fork and never writes a password or token to a log or argv."
    }
  ],
  "toolchain": {
    "image": "ghcr.io/utility-muffin-research-kitchen/mlp1-toolchain:latest",
    "digest": "sha256:66aac16fb8b07e663c9b4d66970f272df195a6eba98dfad8286eabbaa617faf9",
    "cross_prefix": "aarch64-buildroot-linux-gnu",
    "target": "aarch64 Cortex-A55, glibc 2.38 (Miniloong Pocket 1 / RK3566)"
  },
  "device": {
    "glibc_ceiling": "2.38",
    "allowed_libraries": "standalone/device-libs.txt",
    "checked": "2026-09-15 on an MLP1 running Leaf 0.12.0-beta.1 (LoongOS rootfs; glibc 2.38, SDL 2.28.5, both matching the toolchain sysroot for the core runtime libraries)"
  },
  "artifact": {
    "file_name": "dsperate",
    "sha256": "74c2ce5d0242fe529c951f1ec8765a37529ba70d45023db5cd2a890d47ee8aaa",
    "size_bytes": 4472344,
    "note": "Recorded after two clean FORCE=1 PGO builds agree on the v2.1.1 candidate. CI is the independent second-machine verifier; investigate any mismatch rather than overwriting this value."
  },
  "notice": {
    "file_name": "dsperate-notice",
    "sha256": "c52bf4d447c5c855dd02dfb24d8eef962a5d3d079c15b3e4438ae2a5df34a160",
    "size_bytes": 14224,
    "source": "standalone/notice/notice.c",
    "license": "MIT (this repository)",
    "link": "libSDL2-2.0.so.0 and libSDL2_ttf-2.0.so.0, both device-provided; no bundled libraries or fonts",
    "note": "The pak's own fullscreen message program. Built in the same pinned image and verified with the same allowlist. Recorded after two clean FORCE=1 builds agree."
  }
}
